<UserControl x:Class="gloBUS_Music.MVVM.Controls.NavigationMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="200">

    <UserControl.Resources>
        <!-- Анимация для открытия меню -->
        <Storyboard x:Key="SlideMenuOpen">
            <DoubleAnimation 
                Storyboard.TargetName="NavigationMenuGrid"
                Storyboard.TargetProperty="Width"
                To="200" Duration="0:0:0.3"/>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TracksText" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaylistsText" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>

        <!-- Анимация для закрытия меню -->
        <Storyboard x:Key="SlideMenuClose">
            <DoubleAnimation 
                Storyboard.TargetName="NavigationMenuGrid"
                Storyboard.TargetProperty="Width"
                To="60" Duration="0:0:0.3"/>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TracksText" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.1" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaylistsText" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.1" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>

    <!-- Навигационное меню -->
    <Grid x:Name="NavigationMenuGrid" Width="200" Background="LightGray" HorizontalAlignment="Left">
        <!-- Контейнер для всех элементов -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <!-- Кнопка для сворачивания/разворачивания меню -->
            <Button Content="☰" Width="40" Height="40" Click="ToggleMenu_Click" Background="Gray" Margin="5" HorizontalAlignment="Left"/>

            <!-- Кнопка Tracks -->
            <Button Click="NavigateToTracks_Click" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Иконка -->
                    <materialDesign:PackIcon Kind="MusicNote" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TracksText" Text="Tracks" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Кнопка Playlists -->
            <Button Click="NavigateToPlaylists_Click" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Иконка -->
                    <materialDesign:PackIcon Kind="PlaylistMusic" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock x:Name="PlaylistsText" Text="Playlists" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
